- name: SonarCloud Scan
  uses: SonarSource/sonarcloud-github-action@master
  env:
    SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  with:
    args: >
      -Dsonar.organization=Pankuk0
      -Dsonar.projectKey=lab
      -Dsonar.sources=.
      -X # Esta es la bandera de depuración
